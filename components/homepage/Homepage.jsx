import classes from "./homepage.module.css";

const Homepage = () => {
    return (
        <section className={classes.starting}>
            <h1>Welcome! Please create an account to proceed.</h1>
        </section>
    );
};

export default Homepage;